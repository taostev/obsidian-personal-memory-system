# Memory inbox

这里存放尚未确认的候选记忆和候选触发器。

每个候选项应说明：观察、建议规则、作用域、证据、置信度，以及需要用户做出的决定。

确认后：

- 全局偏好移动或合并到 `10-User/`。
- 项目内容移动或合并到 `20-Projects/<project>/`。
- 触发规则移动或合并到 `30-Triggers/`。
- 拒绝的候选项标记为 `status: rejected`，不要反复建议。
