---
type: learning-progress
scope: domain:java-backend
status: active
confidence: high
source: user-stated
created: 2026-08-04
last_confirmed: 2026-08-04
review_after: 2026-09-01
priority: normal
---

# Java 后端学习进度

## 当前状态

- 当前阶段：JavaGuide Java 基础章节
- 最近学习日期：2026-08-08
- 当前重点：按 JavaGuide 章节学习、总结、举例和提问；项目只作为辅助例子

## 进度记录

### 2026-08-04

- 完成：建立 Java 后端专题模块，并启用总体目标与学习进度分离。
- 遗留：填写总体目标、当前阶段和第一个学习主题。
- 下一步：开始第一个学习专题。

### 2026-08-06

- 完成：确认采用项目优先、问答式、结合代码讲解的学习方式；建立覆盖 Java 后端知识域的题库。
- 完成：回答 P01“这个项目解决什么问题？核心业务主线是什么？”。能够说明线上订单履约平台的目标、`order`/`inventory`/`fulfillment` 三个模块，以及下单→创建订单→预占库存→发送履约消息→付款结束订单的主流程。
- 评价：P01 达到“能讲主流程”的程度，暂评 50%；本次按用户要求不追问实现状态和规划内容。
- 记录规则：已掌握的知识写入 [[concepts]]；每次学习结果、遗留问题和下一步追加到本文件；题库和阶段路线保留在当前项目 `docs/` 文件中。
- 下一步：进入 P02，学习服务拆分的领域边界、数据边界、独立部署收益和分布式复杂度。

### 2026-08-06：P02 服务拆分与领域边界

- 我的回答：三个模块各有业务职责，分别负责订单、库存和履约支付管理；数据库相互独立，模块之间解耦，便于扩展升级且降低相互影响。
- 关键纠正：`fulfillment` 的核心职责是履约，不等同于支付管理；支付结果通常由订单域管理，再通过事件通知履约域。
- 补充理解：模块并非完全没有联系，而是通过 API 和消息协作，因此会引入网络失败、分布式一致性、幂等和补偿问题。
- 评价：已掌握职责拆分、数据库隔离和解耦收益，考虑到跨模块协作边界仍需加强，掌握度暂评 75%。
- 下一步：进入 P03，回答 Gateway 到订单服务的完整请求链路。

### 2026-08-08：P03 Gateway 到订单服务的请求链路

- 我的回答：请求先匹配 `/api` 路径进入 Gateway，再根据 `/orders` 找到 Nacos 中的 order 服务，去掉前缀后转发为 `/orders/{id}`。
- 关键纠正：实际路由谓词匹配 `/api/orders/**`；Nacos 是服务注册与发现；还需要由 LoadBalancer 选择实例，并继续经过订单服务的 Controller、Service、Mapper。
- 评价：主链路正确，术语和后端处理链路需要补充，掌握度暂评 75%。
- 下一步：进入 P04，回答 `platform-common` 的职责和为什么不能独立启动。

### 2026-08-08：P04 `platform-common` 公共模块

- 我的回答：`platform-common` 是所有服务共用的 jar，应该放跨服务的共识内容，例如 error code；不应该放各服务不同的内容，例如订单和库存各自的 status。
- 关键补充：它是普通公共 jar，没有独立启动类和业务端口，不能单独启动；错误码也要区分全局通用错误码和领域专属错误码。
- 评价：已理解公共模块的复用价值和领域边界，掌握度暂评 75%。
- 下一步：学习统一 `Result<T>`、`BizException` 和 `@RestControllerAdvice` 的作用与范围。

### 2026-08-08：统一响应与异常处理

- 我的回答：`Result<T>` 统一接口返回格式，`BizException` 统一业务异常返回格式，`@RestControllerAdvice` 拦截某个服务所有 Controller 抛出的异常，从而统一各服务的信息输出。
- 关键补充：Advice 的全局范围只在当前服务内；`BizException` 主要表示可预期业务失败，系统异常应单独记录和兜底；跨服务异常需要通过 Feign/HTTP 错误协议处理。
- 评价：已掌握三个组件的核心职责和服务内作用范围，掌握度暂评 75%。
- 下一步：学习项目日志应该记录什么，以及如何关联订单号、业务号和 traceId。

### 2026-08-08：对话范围调整

- 用户明确：`java八股学习` 只负责 JavaGuide 章节学习，不负责微服务项目开发或项目掌握度评估。
- 后续方式：按章节讲解、总结、举例和提问；可适度结合微服务项目，但不修改项目代码、配置、计划，也不把项目开发任务混入本对话。
- 职责分离：完成微服务项目、评估掌握分别由独立任务负责。
- 下一步：从 JavaGuide 的 Java 基础章节重新开始。

### 2026-08-08：Java 基础——基本类型与包装类型

- 我的回答：列出了部分基本类型，并理解了基本类型与包装类型的“值类型/类封装”区别。
- 关键纠正：完整 8 种基本类型是 `byte`、`short`、`int`、`long`、`float`、`double`、`char`、`boolean`；包装类大多为 `final`，主要价值还包括泛型支持、对象语义、`null` 和自动拆装箱。
- 遗留：继续巩固包装类型常量池、`==` 与 `equals`、自动拆箱 NPE。
- 下一步：回答包装类型为什么存在，以及为什么集合泛型不能使用基本类型。

### 2026-08-08：包装类型知识点复习

- 学习反馈：用户表示暂时不理解包装类型、泛型和自动装箱的关系。
- 本次讲解重点：基本类型表示值；包装类型是对象。集合底层保存对象引用，所以 `List<int>` 不合法，`List<Integer>` 合法；`Integer i = 10` 会自动装箱，约等于 `Integer.valueOf(10)`。
- 遗留：需要通过简单代码题确认泛型限制、自动拆箱和 `null` NPE。
- 下一步：先回答两个基础检查题，再继续包装类型常量池。

### 2026-08-08：泛型、集合与数组的区别

- 学习反馈：用户进一步询问集合为什么只能保存对象，以及数组是否只能保存基本类型。
- 本次讲解重点：Java 泛型参数当前不能使用基本类型；集合基于泛型并保存对象引用；数组既可以是基本类型数组（如 `int[]`），也可以是对象引用数组（如 `Integer[]`、`String[]`）。
- 关键边界：`List<Integer>` 添加 `int` 时会自动装箱；`int[]` 不涉及泛型，也不会自动变成 `Integer[]`。
- 遗留：需要用代码题确认 `List<Integer>`、`int[]`、`Integer[]` 的区别。

### 2026-08-08：自动装箱与 Java 引用

- 我的回答：确认 `int n` 加入 `List<Integer>` 时会自动装箱为 `Integer`，并询问 Java 引用是否等同于 C 指针。
- 关键讲解：Java 引用可以理解为受类型系统和 GC 管理的对象句柄，与 C 指针有间接定位的相似性，但不能做地址运算或直接操作内存，也不需要手动释放。
- 已掌握：自动装箱的基本过程；引用和指针的核心区别仍需通过 `==`、`equals` 和对象共享继续巩固。
- 下一步：学习包装类型常量池，以及 `==` 和 `equals` 的差异。

### 2026-08-08：包装类型缓存与引用比较

- 我的回答：认为 `Integer` 的四个装箱结果都会创建不同对象，因此两次 `==` 都是 `false`。
- 关键纠正：`Integer` 默认会缓存 `-128` 到 `127` 的对象；`127` 的两次自动装箱通常引用同一个缓存对象，所以 `a == b` 为 `true`；`128` 超出默认缓存范围，`c == d` 通常为 `false`。
- 需要记住：包装类型比较数值使用 `equals()`，不要依赖 `==` 的缓存结果。
- 下一步：继续练习 `==`、`equals()` 和自动拆箱的组合场景。

### 2026-08-08：泛型不能使用基本类型

- 我的回答：正确判断 `Integer x = 1000; Integer y = 1000` 中 `==` 为 `false`、`equals()` 为 `true`，并继续询问泛型限制原因。
- 关键讲解：泛型类型参数要求引用类型；泛型运行时通常经过类型擦除并按 `Object` 引用处理，基本类型不是对象；标准集合以对象引用保存元素，所以需要通过包装类型和自动装箱接入。
- 已掌握：包装缓存、`==` 与 `equals()` 的基本区别。
- 遗留：继续巩固泛型擦除、数组与泛型差异，以及自动拆箱 NPE。
- 下一步：学习自动拆箱为 `null` 时为什么会抛出 `NullPointerException`。

### 2026-08-08：自动拆箱与 `null`

- 我的回答：认为 `Integer count = null` 在与 `0` 比较时会默认转换为 `int 0`，因此不输出。
- 关键纠正：包装类型的 `null` 不会转成 `0`；比较表达式会触发自动拆箱，相当于调用 `count.intValue()`，因此直接抛出 `NullPointerException`。
- 补充：成员变量的 `Integer` 默认值是 `null`，`int` 默认值是 `0`；局部变量没有自动默认值，必须初始化。
- 遗留：继续区分成员变量、数组元素和局部变量的默认值。
- 下一步：回答默认值检查题，再进入 String 基础。

### 2026-08-08：成员变量默认值检查

- 我的回答：`Integer a` 的默认值是 `null`，`int b` 的默认值是 `0`。
- 评价：已掌握包装类型、基本类型成员变量的默认值差异，以及局部变量必须初始化的规则。
- 下一步：进入 String 基础，学习不可变性、常量池和字符串比较。

### 2026-08-08：String 不可变性

- 学习反馈：用户表示暂时不理解 String 为什么不可变。
- 本次讲解重点：String 对象创建后内容不能直接改变；对字符串做拼接或替换时，通常是创建新对象并让变量改为引用新对象，原对象不变。
- 讲解要点：不可变性有利于字符串常量池共享、线程安全、哈希值缓存和安全性；频繁拼接应考虑 `StringBuilder`，`StringBuffer` 是带同步的旧方案。
- 遗留：需要通过代码题确认原对象是否改变，以及 `StringBuilder` 与 `String` 的区别。
- 下一步：回答 String 引用重新赋值题。

### 2026-08-08：String 引用重新赋值检查

- 我的回答：`a` 输出 `hello world`，`b` 输出 `hello`，因为 `a` 拼接后引用了新对象，`b` 仍引用原对象。
- 评价：已掌握 String 不可变对象、引用共享和重新赋值的区别。
- 下一步：学习字符串常量池，以及 `==` 与 `equals()` 在 String 上的区别。

### 2026-08-08：String 常量池与线程池辨析

- 我的回答：只确定最后一行 `equals()` 为 `true`，并误把字符串常量池联想到线程池。
- 关键纠正：字符串常量池是 JVM 复用字符串字面量的对象区域，与线程池无关；`a`、`b` 通常引用同一个池中对象，`c` 是 `new` 出来的新对象，所以三行结果是 `true`、`false`、`true`。
- 遗留：继续巩固“字面量复用、显式 new、引用比较、内容比较”的关系。
- 下一步：回答字符串常量池代码检查题。

### 2026-08-20：StringBuilder 与字符串拼接性能

- 我的回答：`String` 不可变，频繁拼接会不断创建新字符串；`StringBuilder` 支持修改，最后再转换成 `String`，可以减少中间字符串创建。
- 关键补充：在循环中使用 `s += "x"`，每次通常都会生成新的 `String`，并复制已有内容，整体可能产生较多临时对象和重复拷贝；`StringBuilder` 内部维护可扩容的字符缓冲区，通过 `append` 直接追加，最后 `toString()` 生成结果字符串。
- 评价：已理解性能差异的核心原因和 `StringBuilder` 的基本解决思路；还需区分编译器对简单拼接的优化，以及 `StringBuilder` 与线程安全的 `StringBuffer`。
- 下一步：回答循环拼接代码题，判断 `StringBuilder` 是否一定只创建一个 `String`。

### 2026-08-22：StringBuilder 循环拼接检查

- 我的回答：循环追加 `0`、`1`、`2` 后，`result` 的值是 `"012"`；`StringBuilder` 负责内部可变缓冲区的追加和扩容，`String` 在最后 `toString()` 时作为不可变结果保存。
- 评价：已掌握 `StringBuilder` 的可变构建阶段与 `String` 的不可变结果阶段。
- 遗留：区分 `StringBuilder` 与 `StringBuffer` 的线程安全和适用场景。
- 下一步：回答多线程场景下是否应该直接使用 `StringBuilder`。

### 2026-08-22：线程安全初步讲解

- 学习反馈：用户对多个线程同时操作同一个 `StringBuilder` 暂时不理解。
- 本次讲解重点：多个线程可能同时执行代码；共享同一个可变对象时，操作可能交错。`StringBuilder` 的 `append` 没有同步保护，因此不保证共享场景下的内容完整性和顺序；单线程或每个线程使用自己的 builder 时通常没有这个问题。
- 补充：`StringBuffer` 的方法带同步，可以降低共享操作的并发风险，但多个线程的整体执行顺序仍不自动确定；需要严格顺序时还要额外协调。
- 遗留：需要通过“共享 builder”和“线程独立 builder”的代码题确认线程安全边界。
- 下一步：回答哪个场景可以安全使用 `StringBuilder`。

### 2026-08-22：StringBuilder 线程安全边界检查

- 我的回答：选择 B，单个方法内部由单个线程使用 `StringBuilder` 是安全的。
- 评价：已理解“是否共享可变对象”是判断线程安全的重要边界；单线程局部使用不需要为线程安全付出额外同步成本。
- 下一步：回答多线程共享场景下 `StringBuffer` 相对 `StringBuilder` 的区别。

### 2026-08-22：StringBuffer 同步机制

- 学习反馈：用户暂时不了解 `StringBuffer` 比 `StringBuilder` 慢的原因。
- 本次讲解重点：`StringBuffer` 的方法通常带同步控制，同一时刻只允许一个线程进入相关操作；这种额外的加锁与并发协调会带来性能开销。`StringBuilder` 没有这层同步，因此单线程下通常更快。
- 遗留：需要通过“是否共享、是否多线程、是否需要顺序保护”三个条件选择 `StringBuilder` 或 `StringBuffer`。
- 下一步：回答一个场景选择题。

### 2026-08-22：StringBuilder 与请求线程的易错点

- 我的回答：在每个 Web 请求内部独立创建 `StringBuilder`、请求之间不共享对象的场景选择了 `StringBuffer`。
- 关键纠正：Web 请求可能由不同线程处理，但只要每个请求使用自己的局部 `StringBuilder`，线程之间就没有共享同一个可变对象，因此应优先使用 `StringBuilder`；是否多线程不是唯一条件，关键是是否共享对象。
- 遗留：继续巩固“线程不同”和“对象共享”不是同一个概念。
- 下一步：用代码判断局部变量和成员变量的共享范围。

### 2026-08-22：局部变量与成员变量的共享范围

- 我的回答：认为 `local` 因为方法可能被外界多线程调用而不安全。
- 关键纠正：有多个线程调用方法不等于它们共享局部变量；每次方法调用都会创建独立的局部 `local`，因此通常安全。`shared` 是对象成员变量，多个线程调用同一个 `Service` 实例时会共享它，操作同一个 `StringBuilder`，因此可能线程不安全。
- 遗留：需要继续巩固“栈上的局部变量独立性”和“对象成员变量共享性”的关系。
- 下一步：回答同一个对象被多个线程访问时，哪些状态需要同步保护。

### 2026-08-22：单例 Service 与成员变量共享

- 我的回答：正确判断单例 `Service` 只有一个对象实例，因此多个请求线程会共享其中的 `shared` 成员变量。
- 评价：已掌握单例对象、成员变量共享和线程安全问题之间的基本关系；同时明确这是 Spring 默认单例作用域下的结论，若改成其他作用域则需重新判断。
- 本小节结论：单线程或线程独占对象使用 `StringBuilder`；多个线程共享同一可变构建对象时，才需要考虑 `StringBuffer`、加锁或其他并发设计。
- 下一步：进入下一个 Java 基础知识点。

### 2026-08-22：hashCode 基础补课

- 学习反馈：用户表示暂时不了解 `hashCode()` 函数。
- 本次讲解重点：`hashCode()` 返回对象的整数哈希值，集合可以先用它快速定位可能的存储位置，再用 `equals()` 做最终内容判断；哈希值不是对象唯一编号，不同对象可能碰撞。
- 遗留：需要理解“`equals()` 相等的对象必须有相同 `hashCode()`”以及只重写其中一个方法会导致 `HashMap`/`HashSet` 行为异常。
- 下一步：回答两个内容相等的 `String` 对象是否具有相同哈希值。

### 2026-08-22：哈希碰撞检查

- 我的回答：正确判断 `hashCode()` 相同不一定代表对象相等，可能发生哈希碰撞。
- 关键补充：两个不同对象也可能通过值相等而使 `equals()` 返回 `true`，例如内容相同的两个 `String`；“不同对象”不等于“逻辑不相等”。
- 评价：已理解哈希碰撞以及 `hashCode()` 不能单独证明对象相等；还需掌握 `equals()` 与 `hashCode()` 的单向约束。
- 下一步：回答如果 `equals()` 相等但 `hashCode()` 不同，会违反什么规则。

### 2026-08-22：equals 与 hashCode 单向约束补课

- 学习反馈：用户暂时不知道 `equals()` 相等但 `hashCode()` 不同时违反的规则。
- 本次讲解重点：Java 约定“如果两个对象 `equals()` 相等，它们必须拥有相同的 `hashCode()`”；否则 `HashMap` 可能把 key 放入一个桶，查询时根据另一个哈希值去其他桶，导致逻辑相等的 key 查找不到。
- 遗留：继续区分“相等必须同哈希”和“同哈希不一定相等”的单向关系。
- 下一步：回答这条规则的正反方向是否都成立。

### 2026-08-22：equals 与 hashCode 单向规则检查

- 我的回答：选择 A，`equals()` 相等一定要求 `hashCode()` 相同。
- 评价：已掌握核心单向约束；明确 `hashCode()` 相同只能说明可能位于同一哈希桶，不能证明 `equals()` 相等。
- 下一步：用自定义对象放入和查询 `HashMap`，理解为什么需要同时重写两个方法。

### 2026-08-22：只重写 equals 导致 HashMap 查询问题

- 我的回答：正确指出只重写 `equals()` 后，逻辑相等对象之间可能出现 `hashCode()` 不相等。
- 关键结论：示例中放入和查询使用的是两个不同的 `User` 对象；如果没有重写 `hashCode()`，它们通常使用 `Object` 的身份哈希值，`HashMap` 可能定位到不同桶，因此查询结果通常为 `null`。
- 评价：已能将 `equals`/`hashCode` 规则应用到 `HashMap` 的 put/get 场景。
- 下一步：学习如何正确同时重写 `equals()` 和 `hashCode()`。

### 2026-08-22：同时重写 equals 与 hashCode 的应用检查

- 我的回答：正确判断当 `equals()` 和 `hashCode()` 都根据 `id` 实现时，`map.get(new User("1001"))` 会返回 `"张三"`。
- 评价：已掌握为什么重写 `equals()` 后通常必须同步重写 `hashCode()`，并能解释逻辑相等 key 的 `HashMap` 查询过程。
- 本小节结论：`equals()` 负责最终逻辑相等判断，`hashCode()` 帮助定位哈希桶；相等对象必须同哈希，但同哈希不一定相等。
- 下一步：学习 `HashSet` 如何利用 `equals()` 和 `hashCode()` 去重。

### 2026-08-22：HashSet 去重检查

- 我的回答：正确判断两个 `id` 相同且正确重写 `equals()`/`hashCode()` 的 `User` 放入 `HashSet` 后，`size()` 为 1。
- 关键理解：`HashSet` 底层依赖哈希结构，先用 `hashCode()` 定位，再用 `equals()` 判断重复，因此相等对象只保留一份。
- 下一步：回答只重写 `equals()` 而不重写 `hashCode()` 时，`HashSet` 是否还能正确去重。

### 2026-08-22：HashSet 只重写 equals 的结果

- 我的回答：正确判断只重写 `equals()` 时不能保证 `HashSet` 去重；如果逻辑相等对象的 `hashCode()` 不同，它们可能进入不同位置并同时保留。
- 关键补充：哈希值不同的情况下，集合通常不会继续调用 `equals()` 跨桶比较，因此不能依赖 `equals()` 单独完成去重。
- 评价：已掌握 `HashMap`/`HashSet` 对 `equals()` 与 `hashCode()` 的共同依赖，本节基础目标完成。
- 下一步：学习可变对象作为 `HashSet` 元素或 `HashMap` key 时的风险。

### 2026-08-22：可变对象作为 HashSet 元素的风险

- 我的回答：意识到元素变化后 `hashCode()` 可能改变，而集合中的位置不会随对象变化自动调整。
- 关键讲解：对象本身仍在集合中，但它原来位于旧哈希桶；修改 `id` 后，`contains(user)` 会根据新哈希值去新桶查找，因此通常返回 `false`，即“对象还在集合里，却找不到它”。
- 评价：已理解哈希集合要求 key/元素的参与 `equals()` 与 `hashCode()` 的字段在放入后保持稳定。
- 下一步：进入下一个 Java 基础知识点。

### 2026-08-22：final 基础开始

- 我的回答：理解 `final` 表示最终形态、不可改变，并询问是否可以继承。
- 关键讲解：`final` 修饰变量表示变量只能赋值一次；修饰类表示类不能被继承；修饰方法表示方法不能被子类重写。变量本身不涉及继承。若 `final` 修饰引用变量，只能保证引用不能改指向，不能保证所引用对象的内部状态不可变。
- 遗留：需要区分 final 基本类型变量、final 引用变量、final 类和 final 方法。
- 下一步：回答 final 引用变量是否能修改对象内部字段。

### 2026-08-22：final 引用变量检查

- 我的回答：正确判断 `final List<String> list` 中重新赋值的最后一行编译错误，因为 `final` 引用不能改变指向；通过 `add()` 修改原对象内容是允许的。
- 评价：已掌握“引用不可变”不等于“对象不可变”的区别。
- 下一步：回答 `final` 类与 `final` 方法分别限制什么。

### 2026-08-22：final 类检查

- 我的回答：正确判断 `final class Parent` 不能被 `Child extends Parent` 继承。
- 评价：已掌握 `final` 修饰类的限制。
- 下一步：回答 `final` 方法能否被子类重写。

### 2026-08-22：final 方法与重写/重载

- 我的回答：正确判断 `final` 方法不能被子类重写，并追问同一个类中是否可以“自己重写”。
- 关键讲解：同一个类不能定义两个完全相同方法签名的方法；子类不能覆盖父类的 `final` 方法，但可以定义同名不同参数的方法，这属于重载，不是重写。子类仍可直接继承并调用父类的 `final` 方法。
- 评价：已掌握 `final` 变量、类、方法的基本限制，以及重写和重载的边界。
- 下一步：总结 `final` 三种用法并进入下一个 Java 基础知识点。

### 2026-08-22：static 静态成员基础

- 我的回答：正确判断 `static int count` 属于 `Counter` 类，而不是某个具体对象，由整个类的对象共享。
- 评价：已理解静态成员只有一份类级存储，通常可以通过 `Counter.count` 访问。
- 下一步：回答静态方法为什么不能直接访问实例成员。

### 2026-08-22：static 方法访问实例成员

- 我的回答：认为静态方法可以直接访问未声明为 `private` 的实例变量，并将默认访问权限误认为 `public`。
- 关键纠正：代码不能编译的根本原因是 `number` 属于具体对象，而 `static print()` 属于类；静态方法没有隐含的 `this`，无法确定要访问哪个对象的 `number`。未写修饰符表示包级访问权限（package-private），不是 `public`。
- 遗留：需要理解静态方法如何通过显式对象引用访问实例成员，以及实例方法为什么可以直接访问静态成员。
- 下一步：回答如何修改代码让静态方法读取某个对象的 `number`。

### 2026-08-22：静态方法显式访问实例成员

- 我的回答：正确判断传入 `Demo demo` 后代码可以编译，但误认为这是通过类名调用变量，并认为静态方法只能调用静态变量。
- 关键纠正：`demo.number` 是通过对象引用访问实例字段，不是通过类名；静态方法可以直接访问静态成员，也可以通过显式对象引用访问实例字段和实例方法，还可以访问自己的局部变量与参数。
- 评价：已理解静态上下文不能隐式访问实例成员，但可以通过对象引用显式访问；访问权限与 static 归属是两个不同概念。
- 下一步：回答实例方法访问静态成员的情况。

### 2026-08-22：实例方法访问静态成员

- 我的回答：误认为实例方法不能直接访问静态变量，并询问是否可以通过类名访问。
- 关键纠正：实例方法可以直接访问静态成员，因为静态成员属于类；也可以使用 `Demo.count` 显式访问。实例方法还可以访问当前对象的实例成员，而静态方法没有隐含的 `this`，不能直接访问实例成员。
- 评价：已补充“实例上下文可以访问类成员，静态上下文不能隐式访问对象成员”的方向性规则。
- 下一步：总结 `static` 的访问规则并进入 static 初始化或工具类场景。

### 2026-08-22：main 方法为什么是 static

- 学习反馈：用户暂时不了解 Java 入口方法为什么需要 `static`。
- 本次讲解重点：JVM 启动程序时还没有创建业务类对象，需要直接通过类名找到并调用 `main`；`static` 让方法属于类，不依赖某个实例。如果 `main` 是实例方法，JVM 需要先知道如何创建对象并确定构造过程，无法作为统一入口。
- 遗留：需要区分“JVM 直接调用静态入口”和“实例方法必须依赖对象”。
- 下一步：回答 `main` 是否可以直接访问普通实例变量。

### 2026-08-22：main 与实例变量

- 我的回答：正确判断静态 `main` 不能直接访问实例变量 `number`，原因与静态方法不能隐式访问实例成员相同；并追问为什么 `main` 被设计成方法。
- 关键讲解：`main` 是一个方法，是因为程序入口需要一个可被 JVM 按固定签名调用的执行单元，可以接收命令行参数并执行启动逻辑；`static` 只表示它不依赖对象，不表示它不是方法。需要访问 `number` 时必须创建 `Demo` 对象或通过对象引用访问。
- 遗留：理解 `main` 的固定签名、命令行参数与应用启动入口之间的关系。
- 下一步：回答如何在 `main` 中合法访问实例变量。

### 2026-08-30：main 中通过对象访问实例变量

- 我的回答：正确判断先创建 `Demo` 对象，再通过 `demo.number` 访问实例变量，可以输出 `10`。
- 评价：已掌握静态入口方法访问实例成员的合法方式：显式创建对象并使用对象引用。
- 下一步：理解 `main(String[] args)` 中 `args` 的来源和作用。

### 2026-08-30：main 方法的命令行参数

- 我的回答：正确指出 `args` 是命令行参数。
- 关键讲解：`String[] args` 是 JVM 启动 `main` 时传入的字符串数组，参数来自运行程序时写在类名或 jar 后面的内容；`args.length` 表示参数个数，参数值通过 `args[0]`、`args[1]` 等访问。
- 遗留：需要注意数组下标从 0 开始，以及未传参数时访问 `args[0]` 会越界。
- 下一步：回答给定命令行参数时 `args.length` 和 `args[0]` 的值。

### 2026-08-30：命令行参数数组检查

- 我的回答：正确判断运行 `java Demo Java Backend` 时，`args.length` 为 `2`，`args[1]` 为 `"Backend"`。
- 评价：已掌握命令行参数按空格分成字符串数组、数组下标从 0 开始的规则。
- 下一步：学习静态变量和静态代码块的初始化时机。

### 2026-08-30：切换为 Java 整体执行流程总览

- 学习请求：用户希望先了解完整的 Java 程序执行流程，再继续 `static` 初始化细节。
- 本次安排：从 `.java` 源码、`javac` 编译、`.class` 字节码、类加载与初始化、`main` 调用，到 JVM 执行引擎、堆栈内存、JIT 和垃圾回收建立全景认识；再回到静态变量和静态代码块。
- 遗留：需要通过简化代码题确认“类加载/初始化”和“对象创建”的先后关系。
- 下一步：回答静态字段、静态代码块和 `main` 的执行顺序。

### 2026-08-30：静态代码块先于 main

- 我的回答：正确判断类初始化阶段先执行静态代码块，之后 JVM 才调用 `main()`。
- 评价：已掌握静态初始化发生在 `main` 之前的基本执行顺序。
- 下一步：理解静态字段默认值、显式赋值和静态代码块按什么顺序执行。

### 2026-08-30：静态字段与静态代码块顺序

- 我的回答：正确判断示例输出为 `2`、`3`。
- 关键理解：静态字段赋值和静态代码块按源代码从上到下执行；后面的静态字段可以读取前面静态代码块更新后的值。
- 评价：已掌握静态初始化中的默认值、显式赋值和代码块顺序关系。
- 下一步：学习创建对象时实例字段、实例代码块和构造方法的执行顺序。

### 2026-08-30：实例初始化与构造方法顺序

- 我的回答：用缩写回答为 `s i s i c`，理解到静态初始化和实例初始化参与执行顺序，但多算了一次静态块和实例块。
- 关键纠正：类初始化只发生一次，静态代码块只执行一次；本题只创建一个 `Demo` 对象，实例代码块和构造方法各执行一次，实际顺序是 `static → instance → constructor`。
- 评价：基本理解类级初始化与对象级初始化的区别；还需通过创建多个对象确认静态块只执行一次而实例块/构造器每次执行。
- 下一步：回答连续创建两个对象时三类代码块的输出次数。

### 2026-08-30：多个对象的初始化次数

- 我的回答：回答为 `1、1、2`，其中第一个 `1` 表示静态代码块一次，后两个数字意图表示实例代码块和构造方法各两次。
- 关键纠正：准确结果是 `static` 执行 1 次，`instance` 执行 2 次，`constructor` 执行 2 次，即 `1、2、2`。
- 评价：已掌握类初始化只执行一次，而实例初始化和构造方法随对象创建次数执行。
- 下一步：学习父类与子类共同存在时的静态和实例初始化顺序。

### 2026-08-30：父类与子类初始化顺序

- 我的回答：输出顺序中漏掉了父类、子类静态代码块，并把父类实例代码块误写成了子类实例代码块。
- 关键纠正：创建 `Child` 对象时，先完成父类静态初始化，再完成子类静态初始化；随后先执行父类实例代码块和构造方法，再执行子类实例代码块和构造方法。完整顺序为 `P-static → C-static → P-instance → P-constructor → C-instance → C-constructor`。
- 遗留：需要通过口诀和更简单的父子类例子巩固“先类后对象、先父后子”。
- 下一步：总结 Java 初始化顺序并进入构造方法与 `this/super`。

### 2026-08-30：super() 构造方法补课

- 学习反馈：用户暂时不了解子类构造方法中默认 `super()` 的作用。
- 本次讲解重点：子类对象包含父类继承来的部分，创建子类对象时必须先调用父类构造方法初始化父类部分；如果子类构造方法没有显式写 `this(...)` 或 `super(...)`，编译器通常会在第一行插入 `super()`。
- 关键边界：如果父类没有无参构造方法，子类必须显式调用存在的 `super(参数)`；`this(...)` 和 `super(...)` 都必须位于构造方法第一行，且不能同时出现。
- 遗留：需要确认父类有参构造方法下，子类如何显式选择构造器。
- 下一步：回答父类只有有参构造方法时子类构造方法如何编写。

### 2026-08-30：显式调用父类有参构造方法

- 我的回答：正确选择在子类构造方法第一行写 `super("string变量")`，显式调用父类的 `Parent(String)` 构造方法。
- 评价：已理解父类没有无参构造方法时，子类必须使用参数匹配的 `super(...)`；并知道构造器调用必须位于第一行。
- 下一步：区分 `this(...)` 调用本类其他构造方法和 `super(...)` 调用父类构造方法。

### 2026-08-30：this() 与 super() 的区别

- 我的回答：正确判断 `this("默认值")` 调用当前类的另一个构造方法。
- 评价：已掌握 `this(...)` 用于构造器之间的复用，`super(...)` 用于调用父类构造器。
- 下一步：回答同一个构造方法中能否同时写 `this(...)` 和 `super(...)`。

### 2026-08-30：this() 与 super() 不能同时出现

- 我的回答：认为构造方法中可以先调用 `this(...)` 再调用 `super(...)`。
- 关键纠正：`this(...)` 和 `super(...)` 都必须是构造方法第一条语句，因此同一个构造方法不能同时写二者；调用 `this(...)` 后，由目标构造方法继续负责调用父类构造方法，最终仍会完成父类初始化。
- 评价：已理解构造器链的基本规则，但需要继续巩固“一个构造器只能选择一条首行调用路径”。
- 下一步：回答 `this(...)` 构造器链最终如何到达父类构造器。

### 2026-09-01：构造器链中的父类构造次数

- 我的回答：认为调用 `new Child()` 时父类构造方法执行 2 次。
- 关键纠正：`Child()` 通过 `this("默认值")` 调用 `Child(String)`，再由后者调用 `super()`；这条链最终只执行一次父类构造方法，不会重复初始化父类部分。
- 评价：已理解 `this(...)` 是构造器转发，构造器链最终只选择一条父类调用路径。
- 下一步：总结对象创建和构造器调用顺序，进入下一个 Java 基础知识点。

### 2026-09-01：构造方法不能继承或重写

- 我的回答：正确判断构造方法不能被子类继承，也不能被重写。
- 关键理解：构造方法用于初始化本类对象，不属于普通可继承行为；子类需要通过 `super(...)` 调用父类构造方法，而不是继承或重写父类构造方法。
- 评价：已掌握构造方法、构造器链、`this(...)` 与 `super(...)` 的基本边界。
- 下一步：总结重载、重写和构造方法的区别，进入面向对象多态。

### 2026-09-01：重写与重载检查

- 我的回答：正确判断相同参数列表、子类重新实现方法体属于重写；同名但参数列表不同属于重载。
- 评价：已掌握重写与重载的核心区分：重写看继承关系和方法签名，重载看同一作用域/类中的参数列表差异。
- 下一步：学习父类引用指向子类对象时，多态调用哪个方法。

### 2026-09-01：运行时多态基础

- 我的回答：正确判断 `Parent obj = new Child(); obj.run();` 输出 `Child`。
- 关键理解：父类引用可以指向子类对象；对于被重写的实例方法，运行时根据对象的实际类型选择子类实现，这就是运行时多态。
- 下一步：比较多态场景下实例方法和成员变量的访问规则。

### 2026-09-01：多态下字段与方法的访问规则

- 我的回答：正确判断多态代码输出 `Parent`、`Child method`。
- 关键理解：成员变量不参与运行时重写，通常按引用的声明类型访问；实例方法如果被子类重写，则按对象的实际类型动态分派。
- 评价：已掌握字段静态绑定与实例方法动态绑定的基本区别。
- 下一步：学习静态方法、私有方法和 final 方法为什么不参与运行时重写。

### 2026-09-01：静态方法不参与运行时多态

- 我的回答：认为 `Parent obj = new Child(); obj.run()` 会调用 `Child` 的静态方法。
- 关键纠正：静态方法不属于对象，也不参与实例方法重写；调用结果通常按引用的声明类型绑定，本题 `obj` 声明为 `Parent`，因此调用 `Parent.run()`。这类子类同名静态方法属于隐藏，不是重写。
- 评价：需要继续区分实例方法的动态分派与静态方法的编译期绑定。
- 下一步：回答通过 `Child.run()` 直接调用时会执行哪个静态方法。

### 2026-09-01：静态方法按类名绑定

- 我的回答：正确判断 `Child.run()` 调用 `Child` 类声明的静态方法。
- 评价：已理解静态方法使用类名调用时按指定类绑定，子类同名静态方法不是运行时重写。
- 下一步：学习 `private` 方法为什么不参与继承和重写。

### 2026-09-01：private 方法不参与重写

- 我的回答：正确判断父类 `call()` 内调用的是父类私有 `run()`，输出 `Parent`。
- 关键理解：`private` 方法对子类不可见，不会被继承；子类定义同名同参方法只是新方法，不构成重写，因此不参与运行时多态。
- 下一步：回答 `final` 实例方法在父类引用指向子类对象时的调用结果。

### 2026-09-01：private 方法调用位置辨析

- 学习反馈：用户追问父类私有方法是否会调用子类同名方法，说明需要区分直接调用和父类内部调用。
- 关键讲解：`new Child().run()` 会调用子类自己的 `run()`；但 `Parent.call()` 内部调用私有 `run()` 时，绑定到父类自己的私有方法，不会动态分派到子类同名方法。
- 评价：已澄清 `private` 方法不参与重写，但调用位置与方法可见性仍需继续通过代码巩固。
- 下一步：回答 `final` 方法在父类引用指向子类对象时的调用结果。

### 2026-09-01：final 方法与多态

- 我的回答：正确判断父类 `final` 方法在 `Parent obj = new Child(); obj.run()` 中输出 `Parent`。
- 关键理解：`final` 方法不能被子类重写，因此不存在子类实现可供运行时动态分派；子类只能继承并调用父类版本。
- 评价：已掌握多态相关的主要特殊情况：普通实例方法支持重写，`static`、`private`、`final` 方法不参与普通运行时重写。
- 下一步：总结面向对象多态，并进入抽象类与接口。

### 2026-09-01：抽象类与抽象方法基础

- 我的回答：理解 `abstract` 表示抽象类；抽象方法只有方法声明、没有方法体，具体子类需要重写实现。
- 关键补充：抽象类可以同时包含普通方法、字段、构造方法和抽象方法，不要求所有方法都是抽象的；如果子类仍是抽象类，可以暂时不实现全部抽象方法，具体子类才必须实现。
- 评价：已掌握抽象方法和子类实现义务的核心概念。
- 下一步：理解抽象类不能直接实例化，以及抽象类构造方法的作用。

### 2026-09-01：抽象类不能直接实例化

- 我的回答：正确判断 `new Payment()` 不能编译，但将原因归为抽象方法尚未重写。
- 关键纠正：根本原因是 `Payment` 被声明为抽象类，抽象类不能直接创建对象；抽象方法是否实现是具体子类能否成为非抽象类、能否被实例化的判断条件。
- 评价：已区分“抽象类不能实例化”和“具体子类必须实现抽象方法”。
- 下一步：回答抽象类是否可以拥有构造方法，以及它何时执行。

### 2026-09-01：抽象类构造方法

- 我的回答：正确判断抽象类可以定义构造方法，创建子类对象时抽象类构造方法会执行。
- 关键理解：抽象类虽然不能直接实例化，但它的构造方法会在子类对象创建过程中通过 `super(...)` 调用，用于初始化父类继承部分。
- 评价：已掌握抽象类不可实例化与抽象类构造方法仍会执行并不矛盾。
- 下一步：学习接口的基本作用及其与抽象类的区别。

### 2026-09-01：接口实现基础

- 我的回答：正确判断 `implements Payment` 表示实现 `Payment` 接口，具体类必须实现 `pay()` 方法。
- 关键补充：如果实现类本身是抽象类，可以暂时不实现全部接口方法；接口中的普通抽象方法默认具有 `public` 访问级别，实现时不能降低可见性。
- 评价：已掌握接口作为行为约定、具体类履行接口方法的基本规则。
- 下一步：比较抽象类和接口的适用场景与核心区别。

### 2026-09-01：单继承与多接口

- 我的回答：正确判断一个类不能继承多个抽象类，但可以实现多个接口。
- 关键理解：Java 类采用单继承，避免多个父类带来的状态和实现冲突；接口主要表达行为契约，因此一个类可以实现多个接口。
- 评价：已掌握抽象类继承数量与接口实现数量的基本规则。
- 下一步：通过场景题选择抽象类或接口。

### 2026-09-03：抽象类与接口场景选择

- 我的回答：正确判断微信支付、支付宝支付和银行卡支付这种只需要统一 `pay()` 行为、没有必须共享状态和公共实现的场景更适合接口。
- 评价：已能根据“行为契约”与“共享状态/实现”选择接口或抽象类。
- 下一步：学习接口字段默认修饰符，以及 Java 8+ 接口方法的不同形式。

### 2026-09-03：接口字段默认修饰符补课

- 学习反馈：用户暂时不了解接口字段的默认性质。
- 本次讲解重点：接口中声明的字段默认是 `public static final`，即公开的类级常量，只能赋值一次，通常通过接口名访问；因此 `Payment.TIMEOUT = 60` 编译错误。
- 补充：接口中的普通方法默认是 `public abstract`；Java 8+ 还支持 `default` 和 `static` 方法，较新的 Java 版本还支持接口内 `private` 辅助方法。
- 遗留：需要区分接口字段常量与实现类实例字段，以及接口中普通方法和 `default` 方法。
- 下一步：回答接口字段是否能通过实现类对象修改。

### 2026-09-03：接口常量不可修改

- 我的回答：正确判断实现类对象不能修改接口字段 `TIMEOUT`，因为它是 `final` 常量。
- 评价：已掌握接口字段默认是 `public static final`，属于接口级常量而非实现类实例属性。
- 下一步：学习接口 `default` 方法的默认实现和重写规则。

### 2026-09-03：接口 default 方法

- 我的回答：正确判断实现类没有重写 `default log()` 时可以直接运行，并输出 `"记录支付日志"`。
- 关键理解：`default` 方法允许接口提供实例方法的默认实现，实现类可以直接继承，也可以按业务需要重写。
- 评价：已掌握 `default` 方法与普通抽象接口方法的基本区别。
- 下一步：回答实现类重写 `default` 方法后调用哪个版本。

### 2026-09-03：实现类重写 default 方法

- 我的回答：正确判断实现类重写 `default log()` 后，调用时执行实现类自己的版本。
- 评价：已掌握 `default` 方法可以被实现类覆盖，重写方法优先于接口默认实现。
- 下一步：学习一个类实现多个接口且默认方法签名冲突时的处理方式。

### 2026-09-03：多个接口 default 方法冲突

- 我的回答：正确判断两个接口存在同名方法时示例不能编译，并提出改方法名。
- 关键纠正：类可以同时实现含有同名方法的多个接口；冲突发生在两个接口都提供相同签名的 `default` 实现时。实现类必须重写该方法明确选择自己的实现，也可以在重写中使用 `A.super.run()` 或 `B.super.run()` 调用指定接口的默认实现；如果能修改接口，改名也是一种设计方案。
- 评价：已理解多接口实现与 default 冲突的区别，以及实现类解决冲突的主要方式。
- 下一步：回答实现类重写冲突方法后如何调用指定接口的 default 实现。

### 2026-09-03：指定接口 default 实现

- 我的回答：正确判断 `A.super.run()` 和 `B.super.run()` 会按代码顺序先后调用 A、B 两个接口的默认实现。
- 评价：已掌握实现类通过 `接口名.super.方法名()` 选择并复用指定接口的 `default` 实现。
- 下一步：学习接口 `static` 方法的调用方式和是否能被实现类继承。

### 2026-09-01：静态方法按类名绑定

- 我的回答：正确判断 `Child.run()` 调用 `Child` 类声明的静态方法。
- 评价：已理解静态方法使用类名调用时按指定类绑定，子类同名静态方法不是运行时重写。
- 下一步：学习 `private` 方法为什么不参与继承和重写。

### 2026-09-01：运行时多态基础

- 我的回答：正确判断 `Parent obj = new Child(); obj.run();` 输出 `Child`。
- 关键理解：父类引用可以指向子类对象；对于被重写的实例方法，运行时根据对象的实际类型选择子类实现，这就是运行时多态。
- 下一步：比较多态场景下实例方法和成员变量的访问规则。

## 记录格式

每次学习结束追加一条记录，包含：主题、完成内容、练习或产出、遗留问题、下一步。不要覆盖历史记录。
